# Cherry needs NO environment variables for its core. Every value here is
# optional, read only by the OPTIONAL local runner, and must live in an
# ignored .env.local or your OS credential store — never in chat or commits.

# Fix the runner pairing token instead of a random one (dev convenience only):
# CHERRY_RUNNER_TOKEN=
