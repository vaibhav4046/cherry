# Cherry compatibility matrix

**Date:** 2026-09-02
**God Mode addendum:** 2026-09-02

| Surface | Status | Notes |
|---|---|---|
| Manual browser (Chrome/Edge/Firefox current) | Working | Full product; e2e-tested on Chromium. |
| WebMCP host (ChatGPT/Codex compatible client) | Implemented, feature-detected | Registers via `document.modelContext.registerTool` with AbortController lifecycle. Verified against the API contract by unit tests with a mock model context, and exercised live on 2026-09-04 in the ChatGPT desktop app (Work mode, model 5.6 Sol): the host called the registered tools through `document.modelContext`, the page changed, and the aperture grew 10 to 11 to 12 as state advanced. Asked to approve a skill, that host enumerated the twelve registered tools and reported that none could. Capture: `docs/release/WEBMCP_LIVE_HOST_CAPTURE.md`. Not yet captured inside a browser host: the full learn-to-approve journey end to end. |
| Unsupported browsers (no WebMCP) | Working | Complete manual product + honest "manual mode" status. |
| PWA install | Implemented | manifest + service worker (static shell only); requires HTTPS host for install prompt. |
| Local runner | Working | `node runner/server.mjs`; 11 runner integration tests (135 across the runner, sandbox, host, executor and bridge suites). Windows/macOS/Linux (pure Node stdlib). |
| Mission Control on the paired runner (outcome to validated graph to parallel sandboxed workers to independent checks) | Working; real Codex captured, Claude Code not captured | `runner/server.mjs` mission routes (`/v2/hosts`, `/v2/missions`); one directory or git-worktree sandbox per task, boundary stated as process or worktree-process; up to three tasks at once; artifacts handed to dependants; succeeded only on the runner's own checks or a person's decision. Runner executor, sandbox and host suites, a browser-to-real-runner integration test and two Playwright journeys. Real Codex CLI 0.152.1 run recorded in `docs/release/GOD_MODE_REAL_HOST_CAPTURE.md`; Claude Code needs a sign-in on this machine; the `--allow-mock-host` rehearsal host is test-only. |
| Native MCP bridge (Claude Code / Codex CLI) | Working, read/verify scope | `node runner/mcp/server.mjs --workspace export.json`; stdio JSON-RPC (initialize, tools/list, tools/call); 6 integration tests. Mutations intentionally excluded (decision D-005). This read/verify surface is distinct from Mission Control worker execution. |
| Codex CLI / MCP | Validated, opt-in | Live Codex CLI read/verify MCP compatibility recorded 2026-09-01. This is distinct from Mission Control worker execution, whose real Codex capture is described above. Provider exit codes never count as verification. |
| Claude Code / MCP | Shipped, opt-in | Live Claude Code read/verify MCP compatibility self-reported 2026-08-29 with no transcript captured, so this row is **Shipped**, not Validated, matching the `/compatibility` page. A 2026-08-30 addendum exists (the skill install into `~/.claude/skills/` and the bridge registration are recorded in the decision log, `docs/CHERRY_DECISIONS.md` D-012, and the addendum in `CHERRY_RELEASE_EVIDENCE.md`; no transcript file). This is distinct from Mission Control worker execution, which remains uncaptured for Claude Code pending a human sign-in. Provider exit codes never count as verification. |
| Optional encrypted sync | Not implemented | Explicitly out of golden v1; no UI pretends otherwise. |
