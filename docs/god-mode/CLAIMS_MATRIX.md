# Claims matrix

Statuses: VALIDATED_REAL (real host or real capture plus deterministic check), SHIPPED_TESTED (code
in the tree with a named green test), EXPERIMENTAL (built, not captured on the real surface),
DESIGNED (specified, not built), NOT_BUILT. Updated at every ticket boundary; the last column is
the commit and date of the last verification. Counts come from the commands named in
`docs/release/GOD_MODE_FINAL_REPORT.md`; nothing here is inferred from a passing type-check.

| Claim | Status | Implementation | Test | Capture | Boundary | Verified |
|---|---|---|---|---|---|---|
| A person can create an outcome-first mission | SHIPPED_TESTED | `mission-control-service.ts` `createMission`, Mission Control composer | `tests/cherry/mission-control.test.tsx`, `tests/cherry/webmcp-god-mode.test.ts`; Playwright `e2e/cherry/god-mode-mission.spec.ts` (see final report for the run) | screenshots `docs/release/screenshots/god-mode/control-*.png` | | branch head, 2026-09-02 |
| Cherry persists a validated acyclic work graph | SHIPPED_TESTED | `mission-plan-model.ts`, `mission-plan-service.ts`, Dexie v6 stores | `tests/cherry/mission-plan.test.ts` (20 shared fixtures, cycle and limit codes), `tests/cherry/god-mode-persistence.test.ts` | | Dexie v6 | branch head, 2026-09-02 |
| At least two tasks run in parallel | VALIDATED_REAL | `runner/lib/mission-executor.mjs`, queue concurrency 1..3 | `runner/mission-executor.test.mjs` (overlapping intervals in the event log; cap of three), `tests/cherry/mission-runner-integration.test.ts` (browser mirror sees two RUNNING) | real Codex: two nodes overlapped (`docs/release/GOD_MODE_REAL_HOST_CAPTURE.md`) | | 2026-09-02 |
| Each repository-writing worker gets its own git worktree | VALIDATED_REAL | `runner/lib/sandbox-manager.mjs` git-worktree provider | `runner/sandbox-manager.test.mjs`, `runner/mission-executor.test.mjs` (worktree dependant starts from the dependency's committed head; source branch untouched) | real Codex: `cherry/mission/<run>/<work item>` branches in the capture | worktree-process, not a VM | 2026-09-02 |
| Work that names no repository still runs in its own sandbox | SHIPPED_TESTED | per-mission scratch root under the approved root | `runner/mission-executor.test.mjs` (scratch root case) | | process | 2026-09-02 |
| Dependants receive what their dependencies produced | SHIPPED_TESTED | artifact collection and materialisation in `mission-executor.mjs`; worktree results committed on the sandbox branch only | `runner/mission-executor.test.mjs` (artifact hand-off; worktree chaining) | | files copied at the same relative paths; no merge into the source branch | 2026-09-02 |
| UI states the actual sandbox boundary | SHIPPED_TESTED | Mission Control node rows (`Workspace: <root> on <branch> from <sha>`, boundary label), landing copy | `tests/cherry/landing-god-mode.test.tsx`; Playwright God Mode specs assert `worktree-process` | | never "VM" | 2026-09-02 |
| Provider completion is not verification | VALIDATED_REAL | adapters return `completed`; executor moves to `verifying`; SUCCEEDED only on a passed report (`mission-orchestrator.ts` refuses otherwise) | `runner/mission-executor.test.mjs` ("exit 0 but a required check fails never reaches succeeded"), `tests/cherry/mission-run-coordination.test.ts`, `tests/cherry/mission-orchestrator.test.ts` | real Codex: the runner's own `node --test` decided both nodes | | 2026-09-02 |
| Independent checks determine the result; a failed check can produce a bounded repair | SHIPPED_TESTED | `runner/lib/checks.mjs`, repair budget and `maxAttempts` at most 3 | `runner/mission-executor.test.mjs` (one repair with the failed check ids, then pass), `tests/cherry/evaluation-service.test.ts` | mock host with `--mock-fail-first` in the Playwright mission spec | | 2026-09-02 |
| Consequential action stops at approval | SHIPPED_TESTED | `policy-service.ts`, `approvePlan` human-only, exact-revision approvals, `decideMissionNode` | `tests/cherry/policy-service.test.ts`, `tests/cherry/mission-plan.test.ts`, `tests/cherry/mission-run-coordination.test.ts`; Playwright mission spec (publish approval waits for a person) | | | 2026-09-02 |
| WebMCP can create, plan and start a mission through bounded tools; no tool can approve | SHIPPED_TESTED | `src/cherry/webmcp/mission-tools.ts`, `control` surface | `tests/cherry/webmcp-god-mode.test.ts` (10 cases incl. injection refusal), `e2e/cherry/webmcp-god-mode.spec.ts` | mock host only | live ChatGPT: EXPERIMENTAL | 2026-09-02 |
| Every WebMCP call appears in Agent View | SHIPPED_TESTED | `registration-manager.ts` call log | `tests/cherry/webmcp.test.ts`, `e2e/cherry/webmcp-god-mode.spec.ts` | | | 2026-09-02 |
| Works with Codex (real execution captured) | VALIDATED_REAL | `runner/lib/agent-hosts.mjs` codex host (`codex exec --sandbox workspace-write`, explicit) | `runner/agent-hosts.test.mjs` (argv from observed flags) | `docs/release/GOD_MODE_REAL_HOST_CAPTURE.md`: codex-cli 0.152.1, ChatGPT sign-in, two worktrees, overlap yes, mission succeeded | process / worktree-process | 2026-09-02 13:39Z |
| Works with Claude Code (real execution captured) | EXPERIMENTAL | claude host built and probed (2.1.224 on this machine) | `runner/agent-hosts.test.mjs` | none: `claude -p` returned 401 OAuth access token revoked; a sign-in is human-only | | 2026-09-02 |
| Works in ChatGPT (live site tools) | EXPERIMENTAL | webmcp | mock-host e2e only | none | | 2026-09-02 |
| Runs while the paired runner is online | SHIPPED_TESTED | runner queue, executor, browser mirror | `runner/runner.test.mjs`, `tests/cherry/mission-runner-integration.test.ts` | | never "24/7"; nothing runs when the runner is off | 2026-09-02 |
| Runs when the laptop is closed | NOT_BUILT | | | | forbidden claim | |
| Connected to LinkedIn, Gmail, YouTube publishing | NOT_BUILT | capability catalogue marks these Roadmap; recipes are draft text only | `tests/cherry/automation-recipes.test.ts` | | forbidden claim | |
| Learns from YouTube | SHIPPED_TESTED | source/watch modules (unchanged) | golden e2e, creators e2e | | transcript, embed, RSS metadata only; no video or caption download | 6e763c5 |
| Approved skills are recommended to a visiting agent | SHIPPED_TESTED | library-service.ts, WebMCP globals | `e2e/cherry/showcase-host.spec.ts`, `e2e/cherry/webmcp-god-mode.spec.ts` (reachable from Mission Control) | | | 2026-09-02 |
| Tamper-evident receipts | SHIPPED_TESTED | proof | `tests/cherry/proof.test.ts` | | hashes, not signatures | 6e763c5 |
| Billions of artifacts | FORBIDDEN | `SCALE_DESIGN.md` is architecture only | | | | |
| 1,000 + 1,000 record harness produces reproducible metrics | SHIPPED_TESTED | `runner/lib/storage/*`, `scripts/god-mode/{generate-corpus,run-scale-benchmark,run-chaos}.mjs` | `tests/scale/god-mode-scale.test.mjs` (17 tests; fast 200+200, `CHERRY_SCALE_FULL=1` 1,000+1,000) | `docs/release/GOD_MODE_SCALE_REPORT.md`, `docs/release/benchmarks/god-mode-scale.json`: 2,000 records in 13.5 s, retrieval 3,900/3,900 top-5, byte-identical corpus across processes | a 2,000-record local benchmark, nothing larger | 2026-09-02 |
| Landing repositioned as the open AI workforce, original copy, Cherry-origin imagery | SHIPPED_TESTED | `src/pages/Landing.tsx`, `src/components/marketing/*` | `tests/cherry/landing-god-mode.test.tsx` (8), `e2e/cherry/landing-god-mode.spec.ts` (5, desktop and Pixel 7) | `docs/release/screenshots/god-mode/landing-*.png` | statuses Validated, Shipped, Available, Experimental, Roadmap; never Connected | 2026-09-02 |
